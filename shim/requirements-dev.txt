# shim 本机测试依赖（issue #49 P1-2）：EDM 文档解析库，与 shim/Dockerfile pin 版本一致。
# 用法：pip install -r shim/requirements-dev.txt 后 cd shim && python3 -m unittest discover -s tests
PyMuPDF==1.28.2
python-docx==1.2.0
openpyxl==3.1.5
python-pptx==1.0.2
